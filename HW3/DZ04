


#include <stdio.h>

int main( )
{
	int a,b,c;
	//printf ("Введите 3 числа:\n");
	scanf ("%d%d%d", &a, &b, &c);
	//x=a+b+c;
	printf ("%d+%d+%d=%d\n",a, b, c, a+b+c);
	printf ("%d*%d*%d=%.2f\n",a, b, c, (a+b+c)/3);
	return 0;
}
